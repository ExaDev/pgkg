# Free-tier OpenRouter configuration for zero-cost benchmark runs.
# Rate-limited — use --limit 5 or similar.
# Use: make bench-openrouter-free
# Requires: OPENAI_API_KEY=sk-or-v1-... (OpenRouter key) set in shell — do NOT commit.

PGKG_LLM_PROVIDER=openai
PGKG_LLM_MODEL=deepseek/deepseek-chat-v3-0324:free
PGKG_EXTRACTOR_MODEL=deepseek/deepseek-chat-v3-0324:free
PGKG_JUDGE_MODEL=deepseek/deepseek-chat-v3-0324:free
PGKG_JUDGE_PROVIDER=openai
PGKG_OPENAI_BASE_URL=https://openrouter.ai/api/v1
PGKG_EMBED_MODEL=BAAI/bge-m3
PGKG_RERANK_MODEL=BAAI/bge-reranker-v2-m3
